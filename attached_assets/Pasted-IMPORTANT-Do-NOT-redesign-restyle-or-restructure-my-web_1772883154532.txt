IMPORTANT: Do NOT redesign, restyle, or restructure my website.

You previously changed the layout incorrectly. I want a very small, controlled update only.

GOAL:
1. Keep my current homepage layout exactly as it is.
2. Keep my current colors, spacing, typography, sections, and styling exactly as they are.
3. Do NOT change the hero, navbar styling, buttons, footer styling, page width, alignment, or overall design.
4. Only fix the navigation and footer consistency for the article pages.

TASKS:

1) RESTORE / PRESERVE CURRENT LAYOUT
- Revert any layout changes you made.
- Use the existing homepage layout as the design source of truth.
- Do not introduce a new layout.
- Do not simplify or modernize anything.
- Do not change desktop or mobile styling except where required below.

2) ADD "INSIGHTS" TO NAVIGATION
- Add a nav item called "Insights".
- Link it to the articles index page.
- Add it to both desktop navigation and mobile navigation.
- Do not change the visual style of the navbar.
- Do not change spacing or alignment of the existing nav items more than necessary.

3) MOBILE MENU
- If there is already a mobile menu, only add the "Insights" link inside it.
- If there is no working mobile menu, create a very simple hamburger menu that matches the existing site style.
- Do not redesign the header.
- Keep the mobile menu minimal and visually consistent with the homepage.

4) FOOTER CONSISTENCY ON ARTICLE PAGES
- On every article page and on the insights index page, use the EXACT SAME footer structure and styling as the main homepage.
- Copy the same footer content from the homepage, including:
  - email
  - Instagram
  - any existing footer text
  - same spacing
  - same font sizes
  - same colors
  - same links
- The footer on article pages must visually match the homepage footer exactly.

5) ARTICLE PAGES
- Keep the article page layout simple and aligned with the site style.
- Do not create a completely different template style.
- Keep typography clean and readable.
- Add a link back to the homepage and to the Insights index if appropriate.
- Preserve SEO tags already created.

6) FILE STRUCTURE
Use this structure if not already present:
/insights/index.html
/insights/ai-digital-strategy-sprint.html
/insights/why-landing-pages-fail.html
/insights/ai-marketing-small-business.html
/insights/high-converting-landing-page.html
/insights/future-of-ai-marketing.html

7) VERY IMPORTANT RESTRICTIONS
- Do NOT change the homepage design.
- Do NOT replace the footer with a new one.
- Do NOT invent new branding elements.
- Do NOT change my layout globally.
- Only make the minimum edits needed:
  a) add Insights to desktop nav
  b) add Insights to mobile nav
  c) make article pages use the same footer as homepage

Before saving, verify:
- homepage still looks the same as before
- mobile menu includes Insights
- article pages include the same footer as homepage
- email and Instagram appear in the footer on article pages