services:
  - type: web
    name: method-strategy-serve
    env: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    nodeVersion: 20
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
